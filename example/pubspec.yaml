name: flutter_share_example
description: Demonstrates how to use the flutter_vesti_share plugin.
publish_to: 'none'

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_vesti_share:
    path: ../

flutter:
  uses-material-design: true
